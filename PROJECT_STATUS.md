# Project Status

## 1. 项目边界

### 项目目标

以 Kaggle Coupon Purchase Prediction 数据验证“会员 × 活动/卡券”的匹配与 Top-K 排序流程，为后续加油站会员营销推荐提供可迁移原型。当前尚未用真实加油站数据验证效果。

### 当前输入

竞赛用户、卡券、购买、浏览及提交样例数据。真实加油站会员、活动、曝光、领取和核销数据的可用性待确认。

### 当前输出

为用户对候选卡券打分、排序，并输出推荐列表；Kaggle 版本生成 `USER_ID_hash` 与 `PURCHASED_COUPONS` 两列提交文件。HTTP 原型接口返回 `activity_id`、`score` 和简要 `reasons`。

### 当前负责范围

样本构造、特征工程、推荐排序、本地验证和推荐接口原型。

### 当前不负责范围

实际发券与资格校验由业务系统负责；真实业务效果与上线流程尚未验证。

## 2. 当前算法实现

`竞赛数据 → 按周构造用户 × 候选券样本 → 历史与卡券特征 → LightGBM 打分 → 降序排序 → Top-10`。

- **数据集**：Kaggle Coupon Purchase Prediction；代码从本地 CSV 或 CSV ZIP 读取。训练脚本使用时间切分，保留 2012-06-17 开始的一周作验证，测试券排序使用该周之后的历史。
- **正样本**：目标周内真实购买的用户–候选券对，去重。
- **负样本**：同周候选券中未购买的用户–券对，每用户最多随机采样 8 个；当前代码未实现困难负样本。
- **用户特征**：年龄、性别、地区、历史购买与浏览次数、距上次购买天数、历史平均购买价格。
- **活动特征**：卡券类别、展示类型、地区、折扣和价格、历史券及类别热度。
- **交叉特征**：用户与券地区是否匹配、价格与用户历史均价距离、用户对该类别的购买与浏览次数、同券购买次数。
- **模型与输出**：LightGBM 二分类目标输出匹配分数，按分数降序取 Top-10；该分数不能直接解释为真实购买概率。代码还包含热度、规则及 Surprise SVD 对照。自定义加油站活动默认走规则评分，尚无用真实业务数据训练的 LightGBM 模型。

## 3. 当前实验结果

| Version | Model | Main Change | Local MAP@10 | Kaggle Public | Notes |
| --- | --- | --- | ---: | ---: | --- |
| 待确认（旧两周实验） | Popularity | 热度基线 | 0.001442 | Pending | 全部 22,873 位验证用户 |
| 待确认（旧两周实验） | Rules | 规则基线 | 0.004651 | Pending | 同一验证集 |
| 待确认（旧两周实验） | Surprise SVD | 协同过滤对照 | 0.000412 | Pending | 同一验证集 |
| 待确认（旧两周实验） | LightGBM | 随机负样本 | **0.010108** | Pending | 当前有记录的最佳本地分数 |
| 待确认（50 周版本） | LightGBM | 扩大训练周数 | Pending | Pending | 仅有 10 用户 smoke test，不能与完整验证比较 |

旧两周结果来自本地 `model_outputs/validation_report.json`；`README.md` 也记录了四种方法的对照。Kaggle 公榜记录目前为空，不能据此判断公榜最佳版本。

## 4. 当前结论

- 旧两周完整本地验证中，LightGBM 的 MAP@10 为 0.010108，高于三个已记录的对照方法。
- 最新训练脚本已改为 50 个完整周训练、留出一周验证并在最后加入第 51 周重训；目前仅有小样本运行报告，完整验证结果仍待运行。
- 现有训练使用随机负样本；同类别困难负样本、真实业务数据训练和线上效果尚无实验依据。

## 5. 下一步

1. 完整运行 50 周版本，记录全用户本地 MAP@10 与运行配置。
2. 如已提交 Kaggle，按实际公榜结果逐次追加到 `results/kaggle_log.csv`。
3. 在固定数据、划分、样本和特征的条件下验证下一项单独改动，例如负样本策略。
